# Meyro Gen updates

Client binaries only (`MeyroGen.zip` = Meyro Gen.exe + version.txt). **No source code. No Update.exe in the zip** — Update.exe is a separate GitHub Release asset fetched only when an update runs.

Token for clients: fine-grained PAT → this repo only → Contents Read-only.
